
1、压缩

```bash
1.tar 命令 （默认打包到当前位置）
	-z 使用gzip压缩
	-c 压缩
	-x 解压
	-v 显示过程
	-f 指定压缩包文件名
	-C 指定压缩、解压位置 （这里是指切换到要压缩的路径，从而直接压缩，不含路径）
	-t 查看压缩包中的文件
	tar -zcvf 名称.tar.gz 文件1 文件2 文件3
	
	压缩的目的：
		a.节省磁盘空间
		b.节省传输流量
		c.减少磁盘IO，降低服务器负载
	
	压缩的时机：
		a.业务的低谷期
```